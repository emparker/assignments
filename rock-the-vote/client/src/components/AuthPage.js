import React from 'react'
import AuthForm from '../forms/AuthForm.js'

export default function AuthPage(){
    // const initInputs = { username: "", password: "" }
        // state:
            //inputs  //toggle

        //handle change to inputs with an(e)
        //handle login
        //handle signup

    return (
        <div>
            test auth.js
            {/* toggle: login <AuthForm />  - signup <AuthForm /> */}
            {/* onClick to set toggle with already a mumber?  */}
        </div>
    )
}